{
  "name": "blog",
  "version": "1.0.0",
  "description": "Jekyll blog for tech articles",
  "scripts": {
    "serve": "bundle exec jekyll serve",
    "build": "bundle exec jekyll build",
    "new-post": "echo 'Create a new file in _posts folder'"
  }
}
